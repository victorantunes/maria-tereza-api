import { ValidationPhoneNumberPipe } from './validation-phone-number.pipe';

describe('ValidationPhoneNumberPipe', () => {
  it('should be defined', () => {
    expect(new ValidationPhoneNumberPipe()).toBeDefined();
  });
});
